function processNotify() {
    pushContent(pullContentFromCalendar("On This Day! 🎉", calendar_id_team, creator_team));
    pushContent(pullContentFromCalendar("🏝️ Leave Today", calendar_id_org, creator_org));
}